○2026/08/28
  MarkdownPluginをfork。pegdown/parboiledからflexmark-java 0.64.8に移行
  （fenced code blockに対応、実行時バイトコード生成を行うparboiled依存を解消）
  ブラウザプレビューに表の罫線CSSを追加（オプション画面で編集可能）
  mermaidブロック(```mermaid)をプレビューで図として描画できるように対応
  （mermaid.min.jsをローカル同梱）
  依存する19個のjarをMarkdownPlugin.jar 1つに同梱
  JDK11でビルドできるよう対応

○2026/08/29
  ノード数が多いmermaid図がプレビューで縮小されすぎる問題を修正
  （useMaxWidth: falseを指定し原寸大で描画、はみ出す分は図の部分
  だけを横スクロールできるようにした）
  mermaid図にドラッグでのpan、Ctrl(Cmd)+ホイールでのzoom機能を追加
  （svg-pan-zoomをローカル同梱）

○2026/08/30
  mermaid図のパン操作をCtrl(Cmd)+ドラッグに変更し、素のドラッグ・
  ダブルクリックはテキスト選択できるようにした
  （svg-pan-zoomのpreventMouseEventsDefaultオプションが有効だと、
  pan/zoomを無効化しても内部でpreventDefault()が呼ばれ続けて
  テキスト選択が阻害されていたため、明示的に無効化）
  mermaid図の表示方式を全面的に見直した
  （横幅に収める縮小はやめ、常に実寸大で表示。横がコンテナ幅を
  超える場合はページに横スクロールバーが出る。縦の高さもズームに
  追随して伸縮し、下限は1行の図が読める70pxのみ）
  拡大縮小・移動の修飾キーをCtrl(Cmd)からAlt(Option)に変更
  （Ctrl+ホイールはブラウザ自体のページ拡大縮小と衝突し、縮小した
  図からマウスが少し外れただけでブラウザ側のズームになってしまう
  ため）

○2026/08/31
  sftp等VFS越しのバッファ（例: sftp://user@host/path/note.md）を
  プレビューすると失敗する問題を修正
  （一時HTMLの作成先にバッファのディレクトリ文字列をそのまま
  java.io.Fileへ渡していたため、VFSのURL文字列がローカルパスとして
  解釈できずエラーになっていた）
  一時HTMLの作成先を、ローカル・VFS越し問わずOS標準の一時ディレクトリ
  に統一（バッファと同じディレクトリに一時ファイルを作らなくなり、
  git等の追跡対象に見える問題も解消）
  Markdown内の相対パス画像を、jEditのVFS経由でローカルにコピーして
  表示できるように対応（画像コピーが必要な場合のみバックグラウンド
  スレッドで実行し、UIをブロックしないようにした）
  ```java・```typescript等のフェンス付きコードブロックにシンタックス
  ハイライトを追加（highlight.jsをローカル同梱。flexmarkが標準で
  出力する class="language-xxx" をそのまま利用できるためJava側の
  レンダリング処理の変更は不要だった）
  コードブロックに背景色・枠線を追加して見た目を目立たせた
  （highlight.jsのテーマCSSの読み込み順をプレビューCSSより前に
  変更し、プレビューCSS側の背景色指定が優先されるようにした上で、
  デフォルトのプレビューCSSに背景色・枠線を追加）
  Markdown内の相対リンク（<a href>）も画像と同様にVFS経由でローカル
  にコピーして機能するように対応（#fragment・?queryは除いた部分を
  コピー対象にし、hrefそのものは書き換えない。ページ内アンカーは
  対象外）
  相対パスに../等が含まれる場合、コピー先が一時ディレクトリの外に
  出てしまうパストラバーサルの問題を修正（コピー先が正規化した上で
  一時ディレクトリの配下に収まっているか確認し、外れる場合は拒否
  してログに警告を出す）
  一時HTML・コピーしたリソースを、プレビューごとに専用の一時
  ディレクトリにまとめるよう変更（複数のMarkdownを続けてプレビュー
  した際、同じ相対パス（例: images/foo.png）を参照しているとリソース
  が衝突していた問題を解消）
  オプション画面で「すべての拡張を有効化」を選ぶと、HTML抑制
  （noBlocks/noInline/noHypertext）まで一緒に有効になり、<details>
  等の生HTMLがプレビューで表示されなくなる問題を修正（デフォルトが
  「すべて有効化」だったため、初期状態から常に発生していた）
